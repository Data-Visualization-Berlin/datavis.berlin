{
  "name": "broken-link-checker",
  "version": "0.0.1",
  "scripts": {
    "build": "ncc build blc.js -o bin -m"
  },
  "dependencies": {
    "@actions/core": "^1.6.0",
    "broken-link-checker": "^0.7.8"
  }
}
